#!/bin/bash -e
cat <<EOF
This is the s2i-maven-java S2I image:
To use it, install S2I: https://github.com/s2iservice

Sample invocation:

s2i build <source code path/URL> s2i-maven-java <application image>

You can then run the resulting image via:
docker run <application image>
EOF
